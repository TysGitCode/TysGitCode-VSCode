CREATE DATABASE FINAL-3800;
USE FINAL-3800;

CREATE TABLE IF NOT EXISTS tmitche5_FINAL (
    ProdID INT AUTO_INCREMENT NOT NULL PRIMARY KEY,
    ProdName VARCHAR(255),
    ProdDesc VARCHAR(255),
    Pricing DECIMAL(10,2),
    Inventory INT,
    Total DECIMAL(10,2)
);
