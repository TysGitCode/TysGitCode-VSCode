#This adds all the current changes to be staged in the working directory
git add .

#You can add individual files by typing this 
git add file_name

#To commit the staged files
git commit -m "commit_title"

#To push the changes 
git push origin master